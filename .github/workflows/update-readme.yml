name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: jeelpatel17/paperpixel@master
        env:
          GITHUB_TOKEN: ${{ secrets.a3a3f27e9e4cdd86ecfde6155a2a0dfb1084d1fb }}